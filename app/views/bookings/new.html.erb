<h1>Bookings#New</h1>

<%= simple_form_for([@space, @booking]) do |f| %>
  <%= f.input :start_date, as: :date, html5: true %>
  <%= f.input :end_date, as: :date, html5: true %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back to Spaces#Index', spaces_path, class: "btn btn-default" %>
